meta {
  name: Authenticate user
  type: http
  seq: 1
}

post {
  url: http://localhost:4000/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "john",
    "password": "changeme"
  }
}
